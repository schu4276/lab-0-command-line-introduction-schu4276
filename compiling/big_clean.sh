#!/usr/bin/bash
#the script takes in two inputs, the first the na,e of a compressed 
#tar arhive that contains the files to be processed, 
#The second is the name of a scratch directory 
ARCHIVE_NAME=$1
SCRATCH=$2
